apiVersion: v1
kind: Service
metadata:
  name: {{ include "memory-chart.fullname" . }}-service
  labels:
    {{- include "memory-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "memory-chart.selectorLabels" . | nindent 4 }}
